# Operations — Hide nested Solution Back

**Status**: Placeholder  
**Increment**: More Changes R63

The application remains a local SPA (`npm start` / `npm run build` / `npm test`). Hosts that pass `agentTabs: { enabled: false, doubleClick: true }` no longer see the nested **Solution** pill after enter; the parent breadcrumb owns back.

This increment does not add deployment, monitoring, or new npm publish steps.

| Unit | Status |
|---|---|
| Hide nested Solution Back | COMPLETE |

Host embed: `docs/workflow-builder-ui-embed.md`
