# Performance Test Instructions — U-PAL-01

**N/A / light** — allow-list filter and defaultAgents mapping are in-memory on catalog load (U-PAL-02). This unit only adds merge fields and pure helpers.

No load/stress suite. Optional: `npm test` PBT run counts stay small (≤80).
