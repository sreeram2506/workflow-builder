# Performance Test Instructions — U-HUI-01

**N/A / light** — per-shell `computed` merge of an in-memory feature tree; no new HTTP or NFR load targets.

Optional: confirm `npm start` interaction stays responsive when toggling `[ui]` repeatedly (manual).
