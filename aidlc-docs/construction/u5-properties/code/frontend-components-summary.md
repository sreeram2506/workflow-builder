# Frontend Components Summary — U5 Properties

## Changed
- `RightSidebarComponent` — reactive General + Configuration form; dual-write draft; Save; empty/view states
- `CanvasViewportComponent` — click already-selected node updates Properties focus

## Behavior
- Auto-expand Properties when exactly one node is selected
- Save enabled when edit mode + dirty + valid
- Canvas continues to render GraphStore until Save
