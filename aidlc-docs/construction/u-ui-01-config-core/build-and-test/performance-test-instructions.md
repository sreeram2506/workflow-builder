# Performance Test Instructions — U-UI-01

**N/A / light** — one-time APP_INITIALIZER HTTP GET + in-memory merge. No load/stress suite for this unit.

Optional: confirm cold start still acceptable after adding initializer (manual `npm start`).
