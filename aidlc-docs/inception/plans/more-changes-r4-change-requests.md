# More Changes R4 — Change Requests (direct implement)

**Source**: `more-changes-r4-clarification-questions.md`  
**Answers**: Q1=A,B,D · Q2=A · Q3=A

## Checklist

- [x] Remove SVG icons from canvas nodes — **text initials only**
- [x] Out→out / in→in draft shows **invalid** (not green); connection blocked
- [x] Persist `sourceSide` / `targetSide` so drag-from-top stays on top after connect

## Notes

- Port roles: `right`/`bottom` = out, `left`/`top` = in
- Compatible = different roles (e.g. right→left, top→bottom)
- Library icons left as-is (freeform said remove icons **in nodes**)
